# MaimConfig Configuration Template
# 复制此文件为 .env 并根据需要修改

# 数据库连接 URL
# 在 Docker 容器中运行时，通常不需要设置此项，因为 start_all_docker.sh 会自动注入正确的路径
# 本地开发时可设置为: sqlite+aiosqlite:///./data/MaiBot.db
# DATABASE_URL=sqlite+aiosqlite:////workspace/data/shared/MaiBot.db

# 服务监听地址与端口
HOST=0.0.0.0
PORT=8000
