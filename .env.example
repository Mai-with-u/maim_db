# 数据库配置 - 支持maimconfig格式，默认SQLite
# 复制此文件为 .env 并根据需要修改配置

# 方式1: 使用完整的DATABASE_URL (推荐)
# DATABASE_URL=mysql+pymysql://username:password@localhost:3306/maimbot_api
# DATABASE_URL=postgresql://username:password@localhost:5432/maimbot_api
# DATABASE_URL=sqlite:///data/MaiBot.db

# 默认配置: 如果不设置任何数据库配置，将自动使用SQLite
# SQLite数据文件位置: data/MaiBot.db

# 方式2: 分别设置数据库参数 (maimconfig兼容)
# DATABASE_HOST=localhost
# DATABASE_PORT=3306
# DATABASE_NAME=maimbot_api
# DATABASE_USER=username
# DATABASE_PASSWORD=password

# 方式3: maim_db原有格式
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=ai_saas
# DB_USER=postgres
# DB_PASSWORD=maimbot_2024

# 数据库连接配置
DB_MAX_CONNECTIONS=20
DB_CONNECTION_TIMEOUT=30
DB_TIMEZONE=UTC

# 服务器配置
HOST=0.0.0.0
PORT=8000
DEBUG=false

# 日志配置
LOG_LEVEL=INFO

# API配置
API_V1_PREFIX=/api/v1
API_V2_PREFIX=/api/v2

# 安全配置
SECRET_KEY=your-secret-key-here
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# 应用配置
APP_NAME=MaiMBot API
APP_VERSION=1.0.0

# 如果不设置任何数据库配置，将自动使用SQLite模式
# SQLite数据文件位置: data/MaiBot.db